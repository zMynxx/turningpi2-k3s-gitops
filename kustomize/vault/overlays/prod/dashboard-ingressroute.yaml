apiVersion: traefik.containo.us/v1alpha1
kind: IngressRoute
metadata:
  name: vault-ui 
  namespace: vault
spec:
  entryPoints:
  - web 
  routes:
  - match: Host(`vault.tpi2.local`)
    kind: Rule
    services:
    - name: vault-ui
      namespace: vault 
      kind: Service
      port: 8200
